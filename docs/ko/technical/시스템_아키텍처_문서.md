# SpaceONE OCI 모니터링 웹훅 시스템 아키텍처

## 📋 개요

본 문서는 SpaceONE OCI 모니터링 웹훅 플러그인의 전체 시스템 아키텍처를 정의합니다. 이 시스템은 Oracle Cloud Infrastructure(OCI) Monitoring 서비스에서 발생하는 알람을 SpaceONE Alert Manager로 실시간 전달하는 웹훅 기반 통합 솔루션입니다.

## 🏗️ 전체 시스템 아키텍처

### 고수준 아키텍처 다이어그램
```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Oracle Cloud Infrastructure                        │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────────────┐  │
│  │  OCI Monitoring │    │ OCI Notification│    │    OCI Resources        │  │
│  │                 │    │    Service      │    │                         │  │
│  │  • Alarms       │    │  • Topics       │    │  • Compute Instances    │  │
│  │  • Metrics      │    │  • Subscriptions│    │  • Load Balancers       │  │
│  │  • Thresholds   │    │  • HTTPS Webhook│    │  • Databases            │  │
│  └─────────────────┘    └─────────────────┘    └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTPS Webhook
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        SpaceONE Monitoring Plugin                           │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        gRPC Interface Layer                         │   │
│  │  ┌─────────────────┐              ┌─────────────────────────────┐   │   │
│  │  │  Webhook API    │              │       Event API             │   │   │
│  │  │                 │              │                             │   │   │
│  │  │  • init()       │              │  • parse()                  │   │   │
│  │  │  • verify()     │              │                             │   │   │
│  │  └─────────────────┘              └─────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         Service Layer                               │   │
│  │  ┌─────────────────┐              ┌─────────────────────────────┐   │   │
│  │  │ WebhookService  │              │     EventService            │   │   │
│  │  │                 │              │                             │   │   │
│  │  │ • 초기화 처리    │              │  • 이벤트 파싱              │   │   │
│  │  │ • 검증 로직     │              │  • 데이터 변환              │   │   │
│  │  └─────────────────┘              └─────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Manager Layer                                │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │                    EventManager                             │   │   │
│  │  │                                                             │   │   │
│  │  │  • OCI 메시지 파싱                                          │   │   │
│  │  │  • SpaceONE 이벤트 변환                                     │   │   │
│  │  │  • 데이터 검증                                              │   │   │
│  │  │                                                             │   │   │
│  │  │  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐   │   │   │
│  │  │  │   Incident  │    │ Incident_1_2│    │ IncidentOCI │   │   │   │
│  │  │  │   (Base)    │    │  (Legacy)   │    │  (Future)   │   │   │   │
│  │  │  └─────────────┘    └─────────────┘    └─────────────┘   │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         Model Layer                                 │   │
│  │  ┌─────────────────┐              ┌─────────────────────────────┐   │   │
│  │  │   EventModel    │              │     ResourceModel           │   │   │
│  │  │                 │              │                             │   │   │
│  │  │ • event_key     │              │  • resource_id              │   │   │
│  │  │ • event_type    │              │  • name                     │   │   │
│  │  │ • title         │              │  • resource_type            │   │   │
│  │  │ • severity      │              │                             │   │   │
│  │  └─────────────────┘              └─────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ gRPC
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            SpaceONE Platform                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Alert Manager                                │   │
│  │                                                                     │   │
│  │  • 이벤트 수신 및 처리                                              │   │
│  │  • 알림 규칙 적용                                                   │   │
│  │  • 사용자 알림 발송                                                 │   │
│  │  • 이벤트 히스토리 관리                                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🔧 컴포넌트 상세 설계

### 1. gRPC Interface Layer

#### Webhook API
```python
class Webhook(BaseAPI, webhook_pb2_grpc.WebhookServicer):
    """웹훅 관리 API"""
    
    def init(self, request, context):
        """플러그인 초기화"""
        # 웹훅 URL 검증
        # 메타데이터 반환
        
    def verify(self, request, context):
        """웹훅 연결 검증"""
        # 테스트 요청 전송
        # 연결 상태 확인
```

#### Event API
```python
class Event(BaseAPI, event_pb2_grpc.EventServicer):
    """이벤트 처리 API"""
    
    def parse(self, request, context):
        """OCI 메시지를 SpaceONE 이벤트로 변환"""
        # 메시지 타입 확인
        # 데이터 파싱 및 변환
        # 검증된 이벤트 반환
```

### 2. Service Layer

#### WebhookService
```python
@authentication_handler
@authorization_handler
@event_handler
class WebhookService(BaseService):
    """웹훅 서비스 비즈니스 로직"""
    
    def init(self, params):
        """웹훅 초기화 로직"""
        # URL 유효성 검사
        # SSL 인증서 검증
        # 메타데이터 생성
        
    def verify(self, params):
        """웹훅 검증 로직"""
        # 테스트 페이로드 생성
        # HTTP 요청 전송
        # 응답 검증
```

#### EventService
```python
@authentication_handler
@authorization_handler
@event_handler
class EventService(BaseService):
    """이벤트 서비스 비즈니스 로직"""
    
    def parse(self, params):
        """이벤트 파싱 로직"""
        # EventManager 호출
        # 결과 검증
        # 로깅 처리
```

### 3. Manager Layer

#### EventManager
```python
class EventManager(BaseManager):
    """이벤트 처리 관리자"""
    
    def parse(self, raw_data):
        """OCI 원시 데이터 파싱"""
        # 버전 확인
        # 적절한 Incident 클래스 선택
        # 데이터 변환 및 검증
        
    def _check_validity(self, event_dict):
        """이벤트 데이터 유효성 검사"""
        # EventModel 검증
        # 필수 필드 확인
        # 데이터 타입 검증
```

### 4. Data Processing Components

#### Incident Classes
```python
class Incident:
    """기본 인시던트 처리 클래스"""
    
    def __init__(self, incident_data, version):
        # 기본 필드 매핑
        # 제목 생성
        # 리소스 정보 추출
        
    def _update_title(self):
        """알람 제목 생성"""
        
    def _update_resource(self):
        """리소스 정보 추출"""
        
    def _update_additional(self):
        """추가 정보 처리"""

class Incident_1_2(Incident):
    """버전 1.2 인시던트 처리"""
    # 특화된 처리 로직

class IncidentOCI(Incident):
    """OCI 전용 인시던트 처리 (향후 구현)"""
    # OCI 특화 처리 로직
```

## 🔄 데이터 플로우

### 1. 웹훅 초기화 플로우
```
SpaceONE Platform
    │
    ├─ gRPC: init() 호출
    │
    ▼
WebhookService
    │
    ├─ URL 검증
    ├─ SSL 인증서 확인
    ├─ 메타데이터 생성
    │
    ▼
Response: PluginInfo
```

### 2. 이벤트 처리 플로우
```
OCI Notification Service
    │
    ├─ HTTPS POST: 웹훅 메시지
    │
    ▼
SpaceONE Platform
    │
    ├─ gRPC: parse() 호출
    │
    ▼
EventService
    │
    ├─ EventManager.parse()
    │
    ▼
EventManager
    │
    ├─ 메시지 타입 확인
    ├─ Incident 클래스 선택
    ├─ 데이터 변환
    ├─ 유효성 검증
    │
    ▼
SpaceONE Event
    │
    ▼
Alert Manager
```

### 3. OCI 구독 확인 플로우
```
OCI Notification Service
    │
    ├─ Subscription Confirmation
    │
    ▼
EventManager
    │
    ├─ 메시지 타입 확인
    ├─ 확인 URL 추출
    ├─ HTTP GET 요청
    │
    ▼
OCI: 구독 활성화
```

## 🛡️ 보안 아키텍처

### 1. 인증 및 권한 관리
```
┌─────────────────────────────────────────────────────────────┐
│                    Security Layer                           │
│                                                             │
│  ┌─────────────────┐    ┌─────────────────────────────┐   │
│  │  Authentication │    │      Authorization          │   │
│  │                 │    │                             │   │
│  │  • gRPC Token   │    │  • Role-based Access       │   │
│  │  • SSL/TLS      │    │  • Resource Permissions    │   │
│  │  • Certificate │    │  • API Rate Limiting        │   │
│  └─────────────────┘    └─────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Message Verification                   │   │
│  │                                                     │   │
│  │  • OCI Signature Verification                      │   │
│  │  • X.509 Certificate Validation                    │   │
│  │  • Timestamp Validation                            │   │
│  │  • Message Integrity Check                         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2. 메시지 보안 처리
```python
def verify_message_security(payload, signature, cert_url):
    """메시지 보안 검증"""
    
    # 1. 인증서 다운로드 및 검증
    certificate = download_and_verify_certificate(cert_url)
    
    # 2. 메시지 서명 검증
    is_valid = verify_signature(payload, signature, certificate)
    
    # 3. 타임스탬프 검증
    is_recent = verify_timestamp(payload.get('timestamp'))
    
    # 4. 메시지 무결성 확인
    is_intact = verify_message_integrity(payload)
    
    return is_valid and is_recent and is_intact
```

## 📊 성능 및 확장성

### 1. 성능 요구사항
| 지표 | 목표 값 | 측정 방법 |
|------|---------|-----------|
| 처리량 | 1,000 events/min | 분당 처리된 이벤트 수 |
| 지연시간 | < 100ms | 웹훅 수신부터 SpaceONE 전달까지 |
| 가용성 | 99.9% | 월간 가동 시간 |
| 동시 연결 | 50개 | 동시 처리 가능한 웹훅 수 |

### 2. 확장성 설계
```
┌─────────────────────────────────────────────────────────────┐
│                    Scalability Architecture                 │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                Load Balancer                        │   │
│  │                                                     │   │
│  │  • Request Distribution                             │   │
│  │  • Health Check                                     │   │
│  │  • SSL Termination                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                            │                               │
│         ┌──────────────────┼──────────────────┐            │
│         │                  │                  │            │
│         ▼                  ▼                  ▼            │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    │
│  │   Plugin    │    │   Plugin    │    │   Plugin    │    │
│  │ Instance 1  │    │ Instance 2  │    │ Instance N  │    │
│  └─────────────┘    └─────────────┘    └─────────────┘    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                Shared Components                    │   │
│  │                                                     │   │
│  │  • Redis Cache (Certificate Cache)                 │   │
│  │  • Message Queue (Event Buffer)                    │   │
│  │  • Metrics Store (Performance Monitoring)          │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 3. 캐싱 전략
```python
class CacheManager:
    """캐시 관리자"""
    
    def __init__(self):
        self.certificate_cache = TTLCache(maxsize=100, ttl=3600)  # 1시간
        self.metadata_cache = TTLCache(maxsize=50, ttl=1800)     # 30분
    
    def get_certificate(self, cert_url):
        """인증서 캐시 조회"""
        if cert_url in self.certificate_cache:
            return self.certificate_cache[cert_url]
        
        # 캐시 미스 시 다운로드
        certificate = download_certificate(cert_url)
        self.certificate_cache[cert_url] = certificate
        return certificate
```

## 🔍 모니터링 및 관찰성

### 1. 로깅 아키텍처
```
┌─────────────────────────────────────────────────────────────┐
│                    Logging Architecture                     │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                Application Logs                     │   │
│  │                                                     │   │
│  │  • Request/Response Logs                           │   │
│  │  • Error Logs                                      │   │
│  │  • Performance Logs                                │   │
│  │  • Security Logs                                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                            │                               │
│                            ▼                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                Log Aggregation                      │   │
│  │                                                     │   │
│  │  • Structured Logging (JSON)                       │   │
│  │  • Log Level Management                             │   │
│  │  • Context Correlation                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                            │                               │
│                            ▼                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Monitoring Dashboard                   │   │
│  │                                                     │   │
│  │  • Real-time Metrics                               │   │
│  │  • Alert Notifications                             │   │
│  │  • Performance Analytics                           │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2. 메트릭 수집
```python
class MetricsCollector:
    """메트릭 수집기"""
    
    def __init__(self):
        self.counters = {
            'webhook_requests_total': Counter('webhook_requests_total'),
            'events_processed_total': Counter('events_processed_total'),
            'errors_total': Counter('errors_total')
        }
        
        self.histograms = {
            'request_duration_seconds': Histogram('request_duration_seconds'),
            'event_processing_duration': Histogram('event_processing_duration')
        }
    
    def record_webhook_request(self, duration, status):
        """웹훅 요청 메트릭 기록"""
        self.counters['webhook_requests_total'].inc()
        self.histograms['request_duration_seconds'].observe(duration)
        
        if status != 'success':
            self.counters['errors_total'].inc()
```

## 🚀 배포 아키텍처

### 1. 컨테이너 기반 배포
```dockerfile
# Multi-stage build
FROM python:3.9-slim as builder
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

FROM python:3.9-slim
WORKDIR /app
COPY --from=builder /usr/local/lib/python3.9/site-packages /usr/local/lib/python3.9/site-packages
COPY src/ .
EXPOSE 50051
CMD ["python", "-m", "spaceone.monitoring.interface.grpc.webhook"]
```

### 2. Kubernetes 배포
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oci-webhook-plugin
spec:
  replicas: 3
  selector:
    matchLabels:
      app: oci-webhook-plugin
  template:
    metadata:
      labels:
        app: oci-webhook-plugin
    spec:
      containers:
      - name: plugin
        image: spaceone/oci-webhook-plugin:latest
        ports:
        - containerPort: 50051
        env:
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          grpc:
            port: 50051
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          grpc:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
```

## 🔧 개발 및 테스트 아키텍처

### 1. 개발 환경
```
┌─────────────────────────────────────────────────────────────┐
│                Development Environment                      │
│                                                             │
│  ┌─────────────────┐    ┌─────────────────────────────┐   │
│  │   Local Dev     │    │      Testing                │   │
│  │                 │    │                             │   │
│  │  • Hot Reload   │    │  • Unit Tests               │   │
│  │  • Debug Mode   │    │  • Integration Tests        │   │
│  │  • Mock OCI     │    │  • Performance Tests        │   │
│  └─────────────────┘    └─────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                CI/CD Pipeline                       │   │
│  │                                                     │   │
│  │  • Code Quality Check (Ruff, MyPy)                 │   │
│  │  • Automated Testing                               │   │
│  │  • Security Scanning                               │   │
│  │  • Container Build & Push                          │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2. 테스트 전략
```python
class TestArchitecture:
    """테스트 아키텍처"""
    
    def unit_tests(self):
        """단위 테스트"""
        # 각 컴포넌트 개별 테스트
        # Mock 객체 사용
        # 빠른 실행 속도
    
    def integration_tests(self):
        """통합 테스트"""
        # 컴포넌트 간 상호작용 테스트
        # 실제 gRPC 통신 테스트
        # 데이터베이스 연동 테스트
    
    def end_to_end_tests(self):
        """E2E 테스트"""
        # 전체 플로우 테스트
        # 실제 OCI 환경 연동
        # 성능 및 부하 테스트
```

## 📈 향후 확장 계획

### 1. 다중 클라우드 지원
```
Current: OCI Only
    │
    ▼
Future: Multi-Cloud
    │
    ├─ AWS CloudWatch
    ├─ Azure Monitor  
    ├─ Google Cloud Monitoring
    └─ OCI Monitoring
```

### 2. 고급 기능 추가
- **지능형 알림 필터링**: ML 기반 노이즈 감소
- **자동 복구 액션**: 알림 기반 자동 대응
- **예측 분석**: 트렌드 기반 사전 알림
- **커스텀 메트릭**: 사용자 정의 지표 지원

## 📚 참고 자료

- **SpaceONE Architecture**: [SpaceONE System Architecture](https://spaceone-dev.gitbook.io/spaceone-architecture/)
- **OCI Architecture**: [Oracle Cloud Architecture Center](https://docs.oracle.com/solutions/)
- **gRPC Best Practices**: [gRPC Performance Best Practices](https://grpc.io/docs/guides/performance/)
- **Microservices Patterns**: [Microservices Architecture Patterns](https://microservices.io/patterns/)

---

**문서 버전**: 1.0.0  
**마지막 업데이트**: 2024년 1월 28일  
**아키텍처 버전**: v1
